# random
# random은 난수를 발생시키는 모듈

import random

# 0.0에서 1.0 사이의 실수 중 난수 값을 리턴하는 함수
print(random.random())

# 1에서 10 사이의 정수 중에서 난수 값을 리턴하는 함수
print(random.randint(1, 10))

# 1에서 55 사이의 정수 중에서 난수 값을 돌려준다
print(random.randint(1,55))